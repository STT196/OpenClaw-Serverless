apiVersion: v1
kind: Service
metadata:
  name: keda-add-ons-http-interceptor-proxy
  namespace: openclaw
spec:
  type: ClusterIP  # Changed from ExternalName
  ports:
    - port: 8080
      targetPort: 8080